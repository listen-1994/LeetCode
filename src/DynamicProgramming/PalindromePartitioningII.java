package DynamicProgramming;

public class PalindromePartitioningII {
    boolean[][] sav;

    public int minCut(String s) {
        sav = new boolean[s.length()][s.length()];
        return 0;
    }
}
